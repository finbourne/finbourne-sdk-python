# coding: utf-8

"""
    Merged OpenAPI Specification

    Merged OpenAPI spec from multiple files  # noqa: E501

    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""

from __future__ import annotations
import pprint
import re  # noqa: F401
import json
from typing import Optional, List, Dict, Union, Annotated, Tuple, Any, ClassVar, TYPE_CHECKING
from datetime import datetime
from uuid import UUID



from pydantic import StrictStr, Field, BaseModel, StrictInt, StrictBool, StrictFloat, StrictBytes, ConfigDict, field_validator, conlist 
from finbourne.sdk.services.lusid.models.scenario_shift_definition import ScenarioShiftDefinition


class ModelOptionShiftDefinition(ScenarioShiftDefinition):
    """
    A shift of a pricing model option for the duration of the scenario. Unlike every other shift  type, the target is not a piece of market data: it is a field of the model options carried by  the recipe's model rule (for example the short-rate volatility of the Hull-White one-factor  lattice), which no market data shift can reach because a model option is configuration, not a  resolved market element. The shift is scoped to a model rule by the model's name, optionally  narrowed to one instrument type, and applies to every instrument that rule prices.  # noqa: E501
    """
    model_name:  StrictStr = Field(...,alias="modelName", description="The pricing model whose options this shift targets, exactly as named on the recipe's model  rule, e.g. \"HullWhite1F\". Only models with shiftable options are accepted; an unknown or  unsupported model name is rejected when the scenario is stored.") 
    instrument_type:  Optional[StrictStr] = Field(default=None,alias="instrumentType", description="The instrument type narrowing which of the model's rules the shift applies to, matching the  instrument-type addressing of model rules in the recipe, e.g. \"ComplexBond\". Omitted, the  shift applies to every instrument the named model prices.") 
    option_name:  StrictStr = Field(...,alias="optionName", description="The model option field the shift moves, e.g. \"Volatility\" or \"MeanReversion\" for  HullWhite1F. Only a whitelisted set of options per model is shiftable; an unknown option  name is rejected when the scenario is stored.") 
    ccy:  Optional[StrictStr] = Field(default=None,alias="ccy", description="For options carrying per-currency overrides (e.g. HullWhite1F's VolatilityByCurrency): the  three to five letter currency code whose effective value the shift moves. The shifted entry starts from the  existing override for that currency, or from the scalar option where no override exists.  Omitted, the shift moves the scalar option and every per-currency override together, so the  effective value moves for every instrument regardless of which level supplies it.") 
    amount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="The size of the shift, in the units given by ShiftType: the option's own units for Absolute  (0.0010 on a volatility of 0.008 is ten basis points of annualised volatility), or a  fraction of the configured value for Relative (0.1 raises it by ten percent).")
    shift_type:  StrictStr = Field(...,alias="shiftType", description="Available values: Absolute, Relative.") 
    scenario_shift_type:  StrictStr = Field(...,alias="scenarioShiftType", description="Available values: RateCurveShiftDefinition, FxShiftDefinition, PriceShiftDefinition, VolSurfaceShiftDefinition, MdkrGroupShiftDefinition, InflationCurveShiftDefinition, CreditSpreadShiftDefinition, ModelOptionShiftDefinition.") 
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["scenarioShiftType", "modelName", "instrumentType", "optionName", "ccy", "amount", "shiftType"]

    @field_validator('shift_type')
    def shift_type_validate_enum(cls, value):
        """Validates the enum"""

        # Finbourne removed enum validation on all models except the
        # oneOf-discriminator case: each oneOf variant declares a `type`
        # field whose enum has exactly one allowable value, which pydantic
        # uses to route the union. We detect that shape here (property
        # named `type`, single allowable value) — no manual class list.

        if "shift_type" != "type":
            return value

        _allowed = ['Absolute', 'Relative']
        if len(_allowed) != 1:
            return value
        if value not in _allowed:
            raise ValueError(f"must be one of enum values {_allowed}")
        return value

    @field_validator('scenario_shift_type')
    def scenario_shift_type_validate_enum(cls, value):
        """Validates the enum"""

        # Finbourne removed enum validation on all models except the
        # oneOf-discriminator case: each oneOf variant declares a `type`
        # field whose enum has exactly one allowable value, which pydantic
        # uses to route the union. We detect that shape here (property
        # named `type`, single allowable value) — no manual class list.

        if "scenario_shift_type" != "type":
            return value

        _allowed = ['RateCurveShiftDefinition', 'FxShiftDefinition', 'PriceShiftDefinition', 'VolSurfaceShiftDefinition', 'MdkrGroupShiftDefinition', 'InflationCurveShiftDefinition', 'CreditSpreadShiftDefinition', 'ModelOptionShiftDefinition']
        if len(_allowed) != 1:
            return value
        if value not in _allowed:
            raise ValueError(f"must be one of enum values {_allowed}")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def __str__(self):
        """For `print` and `pprint`"""
        return pprint.pformat(self. model_dump(by_alias=False))

    def __repr__(self):
        """For `print` and `pprint`"""
        return self.to_str()

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self. model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> ModelOptionShiftDefinition:
        """Create an instance of ModelOptionShiftDefinition from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, by_alias=True):
        """Returns the dictionary representation of the model"""
        _dict = self. model_dump(by_alias=by_alias,
                          mode='json',
                          exclude={
                            "additional_properties"
                          },
                          exclude_none=True)
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if instrument_type (nullable) is None
        # and model_fields_set contains the field
        if self.instrument_type is None and "instrument_type" in self.model_fields_set:
            _dict['instrumentType'] = None

        # set to None if ccy (nullable) is None
        # and model_fields_set contains the field
        if self.ccy is None and "ccy" in self.model_fields_set:
            _dict['ccy'] = None

        # set to None if amount (nullable) is None
        # and model_fields_set contains the field
        if self.amount is None and "amount" in self.model_fields_set:
            _dict['amount'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> ModelOptionShiftDefinition:
        """Create an instance of ModelOptionShiftDefinition from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return ModelOptionShiftDefinition.model_validate(obj)

        _obj = ModelOptionShiftDefinition.model_validate({
            "scenario_shift_type": obj.get("scenarioShiftType"),
            "model_name": obj.get("modelName"),
            "instrument_type": obj.get("instrumentType"),
            "option_name": obj.get("optionName"),
            "ccy": obj.get("ccy"),
            "amount": obj.get("amount"),
            "shift_type": obj.get("shiftType")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj

ModelOptionShiftDefinition.model_rebuild()

