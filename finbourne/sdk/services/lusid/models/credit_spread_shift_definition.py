# coding: utf-8

"""
    Merged OpenAPI Specification

    Merged OpenAPI spec from multiple files  # noqa: E501

    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""

from __future__ import annotations
import pprint
import re  # noqa: F401
import json
from typing import Optional, List, Dict, Union, Annotated, Tuple, Any, ClassVar, TYPE_CHECKING
from datetime import datetime
from uuid import UUID



from pydantic import StrictStr, Field, BaseModel, StrictInt, StrictBool, StrictFloat, StrictBytes, ConfigDict, field_validator, conlist 
from finbourne.sdk.services.lusid.models.scenario_shift_definition import ScenarioShiftDefinition


class CreditSpreadShiftDefinition(ScenarioShiftDefinition):
    """
    A shift of a credit spread curve, targeted by the ticker of the reference entity and,  optionally, the currency the curve is quoted in. The shift applies to the par spread quotes  the curve carries, so a basis-point amount means basis points of spread - the units a spread  shock is quoted in. Shift shapes, tenor windows, scales and the Tent pivot behave exactly as  they do on a rate curve shift.  # noqa: E501
    """
    ticker:  StrictStr = Field(...,alias="ticker", description="The ticker of the reference entity whose spread curve is shifted.") 
    ccy:  Optional[StrictStr] = Field(default=None,alias="ccy", description="The currency the curve is quoted in; disambiguates a ticker quoted in more than one  currency. Omitted, the shift matches the ticker in every currency.") 
    amount: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="The size of the shift, in the units given by Scale: basis points of spread by default  (50 means +50bps), or a percentage of each spread when Scale is Percentage (1 means  spreads scaled by 1.01).")
    start_tenor:  Optional[StrictStr] = Field(default=None,alias="startTenor") 
    end_tenor:  Optional[StrictStr] = Field(default=None,alias="endTenor") 
    shift_type:  StrictStr = Field(...,alias="shiftType", description="Available values: Parallel, Steepen, Flatten, Twist, Tent.") 
    scale:  Optional[StrictStr] = Field(default=None,alias="scale", description="Available values: Bps, Percentage.") 
    pivot_tenor:  Optional[StrictStr] = Field(default=None,alias="pivotTenor", description="The tenor the Tent shift peaks at. The shift applies with the full Amount at this tenor,  falling linearly to zero at StartTenor and EndTenor - the key-rate triangle shape. Only  valid with ShiftType Tent; omitted, a Tent peaks at the midpoint of the window. Declared  last on purpose: generated SDKs emit their positional constructor in property-declaration  order, and this property must not shift the parameters of the ones before it.") 
    scenario_shift_type:  StrictStr = Field(...,alias="scenarioShiftType", description="Available values: RateCurveShiftDefinition, FxShiftDefinition, PriceShiftDefinition, VolSurfaceShiftDefinition, MdkrGroupShiftDefinition, InflationCurveShiftDefinition, CreditSpreadShiftDefinition.") 
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["scenarioShiftType", "ticker", "ccy", "amount", "startTenor", "endTenor", "shiftType", "scale", "pivotTenor"]

    @field_validator('shift_type')
    def shift_type_validate_enum(cls, value):
        """Validates the enum"""

        # Finbourne removed enum validation on all models except the
        # oneOf-discriminator case: each oneOf variant declares a `type`
        # field whose enum has exactly one allowable value, which pydantic
        # uses to route the union. We detect that shape here (property
        # named `type`, single allowable value) — no manual class list.

        if "shift_type" != "type":
            return value

        _allowed = ['Parallel', 'Steepen', 'Flatten', 'Twist', 'Tent']
        if len(_allowed) != 1:
            return value
        if value not in _allowed:
            raise ValueError(f"must be one of enum values {_allowed}")
        return value

    @field_validator('scale')
    def scale_validate_enum(cls, value):
        """Validates the enum"""

        # Finbourne removed enum validation on all models except the
        # oneOf-discriminator case: each oneOf variant declares a `type`
        # field whose enum has exactly one allowable value, which pydantic
        # uses to route the union. We detect that shape here (property
        # named `type`, single allowable value) — no manual class list.

        if "scale" != "type":
            return value

        if value is None:
            return value

        _allowed = ['Bps', 'Percentage']
        if len(_allowed) != 1:
            return value
        if value not in _allowed:
            raise ValueError(f"must be one of enum values {_allowed}")
        return value

    @field_validator('scenario_shift_type')
    def scenario_shift_type_validate_enum(cls, value):
        """Validates the enum"""

        # Finbourne removed enum validation on all models except the
        # oneOf-discriminator case: each oneOf variant declares a `type`
        # field whose enum has exactly one allowable value, which pydantic
        # uses to route the union. We detect that shape here (property
        # named `type`, single allowable value) — no manual class list.

        if "scenario_shift_type" != "type":
            return value

        _allowed = ['RateCurveShiftDefinition', 'FxShiftDefinition', 'PriceShiftDefinition', 'VolSurfaceShiftDefinition', 'MdkrGroupShiftDefinition', 'InflationCurveShiftDefinition', 'CreditSpreadShiftDefinition']
        if len(_allowed) != 1:
            return value
        if value not in _allowed:
            raise ValueError(f"must be one of enum values {_allowed}")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def __str__(self):
        """For `print` and `pprint`"""
        return pprint.pformat(self. model_dump(by_alias=False))

    def __repr__(self):
        """For `print` and `pprint`"""
        return self.to_str()

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self. model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> CreditSpreadShiftDefinition:
        """Create an instance of CreditSpreadShiftDefinition from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self, by_alias=True):
        """Returns the dictionary representation of the model"""
        _dict = self. model_dump(by_alias=by_alias,
                          mode='json',
                          exclude={
                            "additional_properties"
                          },
                          exclude_none=True)
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if ccy (nullable) is None
        # and model_fields_set contains the field
        if self.ccy is None and "ccy" in self.model_fields_set:
            _dict['ccy'] = None

        # set to None if amount (nullable) is None
        # and model_fields_set contains the field
        if self.amount is None and "amount" in self.model_fields_set:
            _dict['amount'] = None

        # set to None if start_tenor (nullable) is None
        # and model_fields_set contains the field
        if self.start_tenor is None and "start_tenor" in self.model_fields_set:
            _dict['startTenor'] = None

        # set to None if end_tenor (nullable) is None
        # and model_fields_set contains the field
        if self.end_tenor is None and "end_tenor" in self.model_fields_set:
            _dict['endTenor'] = None

        # set to None if pivot_tenor (nullable) is None
        # and model_fields_set contains the field
        if self.pivot_tenor is None and "pivot_tenor" in self.model_fields_set:
            _dict['pivotTenor'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> CreditSpreadShiftDefinition:
        """Create an instance of CreditSpreadShiftDefinition from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return CreditSpreadShiftDefinition.model_validate(obj)

        _obj = CreditSpreadShiftDefinition.model_validate({
            "scenario_shift_type": obj.get("scenarioShiftType"),
            "ticker": obj.get("ticker"),
            "ccy": obj.get("ccy"),
            "amount": obj.get("amount"),
            "start_tenor": obj.get("startTenor"),
            "end_tenor": obj.get("endTenor"),
            "shift_type": obj.get("shiftType"),
            "scale": obj.get("scale"),
            "pivot_tenor": obj.get("pivotTenor")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj

CreditSpreadShiftDefinition.model_rebuild()

