# OverrideVirtualTransactionsRequest

Upserts overrides and suppressions of virtual transactions generated by a single instrument event.  Overrides are keyed by the virtual transaction id being overridden; suppressions are the list of virtual  transaction ids being suppressed. A virtual transaction id must not appear in both. This is a full  replace of the previously stored overrides and suppressions for the targeted instrument event - any  previously stored entry not present in this request is removed.
## Properties

| Name | Type | Required | Description |
|------|------|----------|-------------|
| **overrides** | **Dict[str, Optional[List[OverrideDefinitionRequest]]]** | Optional | A map of virtual transaction id to one or more replacement transactions. A virtual transaction id cannot also appear in Suppressions. |
| **suppressions** | **List[str]** | Optional | The virtual transaction ids to suppress. A virtual transaction id cannot also appear in Overrides. |


## Usage

### Creating from keyword arguments

```python
from finbourne.sdk.services.lusid.models.OverrideVirtualTransactionsRequest import OverrideVirtualTransactionsRequest

instance = OverrideVirtualTransactionsRequest(
    overrides=,  # optional — A map of virtual transaction id to one or more replacement transactions. A virtual transaction id cannot also appear in Suppressions.
    suppressions=  # optional — The virtual transaction ids to suppress. A virtual transaction id cannot also appear in Overrides.
)
```



[Back to top](#) · [Back to API list](../../api_endpoints.md) · [Back to Model list](../../models.md) · [Back to README](../../../../../README.md)

